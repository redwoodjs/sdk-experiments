"use client";

import ReactMarkdown from "react-markdown";

export function Home() {
  return (
    <div>
      <ReactMarkdown>
        # Hello from React Markdown! This is a **basic** example.
      </ReactMarkdown>
    </div>
  );
}
