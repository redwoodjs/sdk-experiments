"use client";
import { jsx as _jsx } from "react/jsx-runtime";

export function TestDepDualClientComponent(props) {
  console.log(
    "================================================== In TestDepDualClientComponent test() for SSR!!! ================================================================",
  );

  if (import.meta.env.RWSDK_ENV !== "worker" && !props.isClientRoot) {
    console.log(
      "#### TestDepDualClientComponent (env=%s): setting inner instance to this instance",
      import.meta.env.RWSDK_ENV,
    );
    globalThis.__TestDepDualClientComponent = TestDepDualClientComponent;
  }

  if (globalThis.__TestDepDualClientComponent && props.isClientRoot) {
  console.log(
      "#### TestDepDualClientComponent (env=%s): does root instance match this instance?: %s",
      import.meta.env.RWSDK_ENV,
      globalThis.__TestDepDualClientComponent === TestDepDualClientComponent,
    );
  }

  globalThis.__TestDepDualClientComponent = TestDepDualClientComponent;
  return _jsx("div", { children: "TestDepDualClientComponent" });
}