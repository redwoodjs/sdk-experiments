// Export everything from utils
export {
  formatString,
  reverseString,
  capitalizeFirst,
  UTILS_CONSTANT
} from './utils.js';

// Export everything from math
export {
  add,
  multiply,
  divide,
  factorial,
  PI,
  E
} from './math.js';

// Export everything from dom
export {
  createElement,
  removeClass,
  addClass,
  setStyle,
  DOM_UTILS_VERSION
} from './dom.js';

// A function that uses multiple modules (to test side effects)
export function complexOperation(str, num) {
  console.log('complexOperation called');
  const formatted = formatString(str);
  const result = multiply(num, PI);
  return `${formatted}: ${result}`;
}

// A default export
export default {
  version: '1.0.0',
  name: 'dummy-tree-shake-test'
};
