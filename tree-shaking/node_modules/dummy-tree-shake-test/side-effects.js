'use client';

// This file has side effects when imported
console.log('Side effects file loaded!');

// Global side effect
globalThis.DUMMY_PACKAGE_LOADED = true;

// Another side effect
if (typeof window !== 'undefined') {
  window.addEventListener('load', () => {
    console.log('Dummy package: Window loaded');
  });
}

export function triggerSideEffect() {
  console.log('Side effect function called');
  return 'side effect triggered';
}
